
    <%= product.name %>
    <%= product.price %>

    <% if product.image.attached? %>
       <%= image_tag(product.image, :size => '100x100') %>
    <% end %>

    <% if !(current_user.role_id == @role.id) %>
         <%= link_to 'Buy', buys_path(:params => {:product_id => product.id, :user_id => current_user.id}), method: :create %> 
    <% end %>
    
    <% if current_user.role_id == @role.id %>
         <%# link_to 'Delete', destroy_path(:params => {:product_id => product.id}), method: :delete %> 
         <%# link_to 'Update'%> 
    <% end %>
<br><br>